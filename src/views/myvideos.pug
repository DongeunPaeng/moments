extends main
include mixins/videoSection

block content
    .container.mx-auto.flex.flex-wrap.justify-center.font-body
        each item in videos
            +videoSection({
                id: item.id,
                creator: item.creator.email,
                title: item.title,
                views: item.views,
                description: item.description,
                fileUrl: item.fileUrl
            })