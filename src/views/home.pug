extends main
include mixins/videoSection

block content
    each item in videos
        +videoSection({
            id: item.id,
            title: item.title,
            views: item.views,
            description: item.description,
            fileUrl: item.fileUrl
        })